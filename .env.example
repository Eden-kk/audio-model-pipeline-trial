# Audio-model trial — deployment-time environment.
#
# Copy to `.env` (next to docker-compose.yml) and fill in.
# `docker compose up -d --build` reads this file automatically.

# ── Public hostname & TLS ────────────────────────────────────────────────────
PUBLIC_HOST=audio-trial.example.com
PUBLIC_ORIGIN=https://audio-trial.example.com
LETSENCRYPT_EMAIL=you@example.com

# ── ROCm — GPU architecture override (model-server, AMD lane only) ─────────
# Leave blank for MI300X / MI250X (auto-detected).
# Consumer Navi 31 (7900 XTX, 7900 XT, 7900 GRE): set to 11.0.0
# Consumer Navi 22 (6700 XT, 6800 XT): set to 10.3.0
HSA_OVERRIDE_GFX_VERSION=

# ── Self-host backend selection ─────────────────────────────────────────────
# Pick ONE of: modal | amd | local.  Default = modal so a fresh clone hits the
# documented Modal deploy. Flip to "local" on a NVIDIA CUDA box (B200, H100,
# A100, etc.) and to "amd" on a ROCm box.
MODEL_SERVER_BACKEND=modal
# Local CUDA URL (native venv via model-server/run-cuda.sh, OR docker via
# model-server/docker-compose.cuda.yml on a docker-enabled host).
MODEL_SERVER_LOCAL_URL=http://localhost:9100
MODEL_SERVER_AMD_URL=http://localhost:9100
# Public Modal URL printed by `modal deploy model-server/modal_app.py`.
MODEL_SERVER_MODAL_URL=
# Where local model weights cache (NeMo + HF). Pick a roomy mount; do NOT
# leave it under $HOME if /home is small. Used by run-cuda.sh + run-vllm.sh.
MODEL_CACHE_DIR=/raid/yid042/audio-trial-models

# ── ASR adapters (cloud) ────────────────────────────────────────────────────
DEEPGRAM_API_KEY=
GLADIA_API_KEY=
ASSEMBLYAI_API_KEY=
SPEECHMATICS_API_KEY=
GROQ_API_KEY=

# ── TTS / speaker-verify ────────────────────────────────────────────────────
CARTESIA_API_KEY=
HF_TOKEN=                  # accept the license at hf.co/pyannote/embedding first

# ── Realtime omni (MiniCPM-o adapter) ───────────────────────────────────────
# The minicpm_o adapter posts to whatever OpenAI-compat-shaped /v1/omni
# endpoint MINICPM_O_REALTIME_URL points at. Three deployments today:
#   • Modal:         `cd model-server && modal deploy modal_app_minicpm.py`
#                    → set the printed URL here.
#   • Local (Path D, NVIDIA box, no docker):
#                    Set ENABLE_MINICPMO=1 below — run-public.sh boots a
#                    native FastAPI server (model-server/server_minicpmo.py)
#                    on $MINICPMO_PORT and auto-wires this URL to
#                    http://localhost:$MINICPMO_PORT.
#   • Empty:         the adapter reports "unset" in /api/settings and the
#                    /realtime page shows it as unconfigured (safe default).
MINICPM_O_REALTIME_URL=

# Path D MiniCPM-o tuning (only consulted when ENABLE_MINICPMO=1).
# Default GPU 7 on this shared B200 (GPUs 0–6 are NeMo / vLLM / other users).
ENABLE_MINICPMO=0
MINICPMO_PORT=9101
MINICPMO_GPU=7

# ── Slow-loop intent LLM (qwen_intent adapter) ──────────────────────────────
# Any OpenAI-compatible /v1 endpoint works. Pick ONE:
#   • Modal:  the existing exp06-qwen25-openai-server URL.
#   • Local:  bash model-server/run-vllm.sh on this host (uses GPU $VLLM_GPU,
#             default 1 — separate from model-server-cuda's GPU 0).
#   • OpenAI: https://api.openai.com/v1 + your key.
INTENT_LLM_URL=
INTENT_LLM_MODEL=Qwen/Qwen2.5-7B-Instruct
INTENT_LLM_KEY=
# Path D vLLM tuning:
VLLM_GPU=1
VLLM_PORT=8001
VLLM_MAX_MODEL_LEN=8192

# ── Storage ─────────────────────────────────────────────────────────────────
DATA_DIR=/app/data         # inside the trial-app container; mapped to volume

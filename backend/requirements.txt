fastapi[standard]==0.115.4
uvicorn[standard]==0.32.0
python-multipart
soundfile
numpy<2
httpx
websockets>=12         # Slice 3 streaming adapters
pydantic>=2.0
python-dotenv

# ASR — self-host
faster-whisper

# ASR — cloud SDKs (most adapters use httpx directly; SDK kept where useful)
deepgram-sdk

# Speaker verify
resemblyzer
pyannote.audio>=3.1
torch
torchaudio
# pyannote 3.x dropped omegaconf from install_requires but its lightning
# checkpoint loader still needs it on import. Keep it explicit so a fresh
# venv doesn't 500 on the first /api/enroll call.
omegaconf

# Realtime omni — Gemini Live (Vertex AI). Uses ADC; no API key in env.
# Unified SDK; supersedes google-generativeai. Provides
# `from google import genai` + `client.aio.live.connect(...)`.
google-genai>=0.4.0

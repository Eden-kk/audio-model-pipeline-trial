fastapi[standard]==0.115.4
uvicorn[standard]==0.32.0
python-multipart
soundfile
numpy<2
httpx
websockets>=12         # Slice 3 streaming adapters
pydantic>=2.0
python-dotenv

# ASR — self-host
faster-whisper

# ASR — cloud SDKs (most adapters use httpx directly; SDK kept where useful)
deepgram-sdk

# Speaker verify
resemblyzer
pyannote.audio>=3.1
torch
